import { ColorchngDirective } from './colorchng.directive';

describe('ColorchngDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorchngDirective();
    expect(directive).toBeTruthy();
  });
});
