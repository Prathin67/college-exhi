import { BtnhighDirective } from './btnhigh.directive';

describe('BtnhighDirective', () => {
  it('should create an instance', () => {
    const directive = new BtnhighDirective();
    expect(directive).toBeTruthy();
  });
});
